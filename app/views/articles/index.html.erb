<% @articles.each do |article| %>
  <img src="https://kitt.lewagon.com/placeholder/cities/random" alt="bill">
  <h2><%= link_to article.title, article_path(article.id)%></h2>
  <p><%= article.content%></h2>
    <hr>
<% end %>
